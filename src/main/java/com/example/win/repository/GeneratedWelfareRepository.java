package com.example.win.repository;

import com.example.win.entity.GeneratedWelfare;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GeneratedWelfareRepository extends JpaRepository<GeneratedWelfare, Long> {
}
